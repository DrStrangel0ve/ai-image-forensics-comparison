# SOTA Gap Closure Plan

Run date: 2026-06-14

Generated by `scripts/build_sota_gap_closure_plan.py` from the guarded SOTA-gap report.

This is an experiment plan, not a claim of leaderboard performance. It ranks the next work by how directly it can turn the current proxy evidence into official or paper-compatible benchmark evidence.

## Current Gap Anchor

- Best NTIRE-style local proxy row: `ms_to_ishu_tuned_fusion_noise3` with AUC `0.8704`.
- Official NTIRE 2026 robust SOTA anchor: AUC `0.9723`.
- Best proxy gap to that anchor: `-0.1019` AUC.
- Stress rows tracked: `5`; weakest tracked gap is `-0.1907` AUC from `ms_to_ishu_tuned_fusion_resize_half`.

## Status Summary

| status | count |
| --- | --- |
| ready_when_data_available | 1 |
| blocked_on_official_labels_or_next_cycle | 1 |
| clip_reverse_pass_complete | 1 |
| implementation_next | 1 |
| data_next | 1 |
| watchlist | 1 |

## Prioritized Tasks

| rank | task_id | target | status | why | primary_gap | exit_criterion | paper_use |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | genimage_official_split_eval | GenImage official or paper-compatible splits | ready_when_data_available | This is the cleanest academic SOTA bridge: a public benchmark family with generator and degradation splits. | No official GenImage run; current evidence is Ishu/MS COCOAI proxy transfer only. | Report per-generator and degraded-split accuracy/AUC for CLIP, DINOv2, combined_v3, physics-guided, and SCP-Fusion. | Turns SOTA comparison from methodological to benchmark-backed. |
| 2 | ntire_released_protocol_replay | NTIRE 2026 released train/validation protocol or NTIRE 2027 | blocked_on_official_labels_or_next_cycle | NTIRE is the strongest robustness SOTA anchor; current best proxy AUC is below the official robust top score. | No official NTIRE score; current NTIRE-style proxy rows are marked `proxy_not_official`. | Produce an official or released-split ROC-AUC table with clean, transformed, and average robust scores. | Needed for any future NTIRE/SOTA leaderboard claim. |
| 3 | high_res_tiled_foundation_eval | Ishu/MS COCOAI plus future GenImage/RealHD high-resolution slices | clip_reverse_pass_complete | The current native-tiling gain only tiles the conventional branch; official SOTA systems often exploit high-resolution local evidence. | CLIP reverse-transfer tiling is now benchmarked; DINOv2/ConvNeXt tiling and source-heldout fused use are still pending. | Fold the strongest tiled foundation operating point into source-heldout SCP-Fusion and test whether it improves AUC beyond 0.8472 or reduces resize/blur stress losses. | Strengthens SCP-Fusion as a forensic, high-resolution protocol instead of a resized-image classifier. |
| 4 | reconstruction_branch_ablation | Ishu/MS COCOAI, then GenImage degraded splits | implementation_next | AEROBLADE/FIRE-style reconstruction signals are a major SOTA direction and are not yet a full branch here. | combined_v4 includes lightweight reconstruction proxies, but no pretrained reconstruction detector branch. | Report whether reconstruction features beat combined_v3 or improve SCP-Fusion transfer/robustness under JPEG, blur, and resize. | Makes the physics/conventional branch more competitive with current reconstruction-based forensics. |
| 5 | source_diverse_training_scaleup | MS COCOAI plus recent generated-image datasets | data_next | SOTA robustness depends heavily on source diversity; current transfer results show strong threshold and generator-shift sensitivity. | Training sources are much narrower than NTIRE-style 42-generator protocols. | Show improved heldout-source AUC/fake-call balance without target tuning. | Separates model weakness from dataset/source-coverage weakness. |
| 6 | imageclef_next_cycle_packaging | ImageCLEF 2027-style no-training/public-submission protocol | watchlist | ImageCLEF rewards robust packaging and clear operating points; our triage evidence is a natural fit. | No ImageCLEF 2026 task data or submission. | A dry-run submission folder can be produced from any image folder with a stable manifest and score CSV. | Makes the repo competition-ready for the next open cycle. |

## Command Notes

| rank | task_id | commands |
| --- | --- | --- |
| 1 | genimage_official_split_eval | Train frozen CLIP/SCP-Fusion branches on the chosen source split, then run `python scripts/evaluate_frozen_encoder_model.py --model-dir <clip_run> --target-dir <genimage_target> --output-dir <out>` and `python scripts/evaluate_feature_model.py --model-dir <combined_v3_run> --target-dir <genimage_target> --output-dir <out>`. |
| 2 | ntire_released_protocol_replay | Once labels/splits are available, run source-aware SCP-Fusion and robustness transforms with the same ROC-AUC reporting; keep official and proxy outputs in separate report IDs. |
| 3 | high_res_tiled_foundation_eval | Run `python scripts/evaluate_tiled_frozen_encoder_model.py --model-template <frozen_run_template> --target-template <target_template> --asset-prefix <prefix> --output-dir <out>` for DINOv2/ConvNeXt next, then compare max/mean/top-k tile pooling against current global scores. |
| 4 | reconstruction_branch_ablation | Add cached autoencoder/diffusion-reconstruction residual features, then evaluate them alone and in SCP-Fusion with source-heldout calibration. |
| 5 | source_diverse_training_scaleup | Expand the dataset catalog, train repeated seeds with `python scripts/run_repeated_benchmark.py --data-dir <source> --out-dir <runs> --seeds 7 17 29 -- ...`, and evaluate leave-one-generator-out. |
| 6 | imageclef_next_cycle_packaging | Package the inference path as a single runner that emits per-image scores, labels, and confidence/triage decisions without training-time leakage. |

## Immediate Recommendation

1. Do `genimage_official_split_eval` first if the data can be downloaded cleanly; it is the fastest way to make the paper's SOTA section concrete.
2. Extend `high_res_tiled_foundation_eval` from the completed CLIP reverse-transfer pass to DINOv2/ConvNeXt and then into fused source-heldout selection.
3. Keep NTIRE/ImageCLEF wording conservative until an official or released-split run exists.
